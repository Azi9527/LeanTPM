# M0 基础能力交付记录

## 1. 阶段状态

M0「基础能力强化」已于 2026-07-30 完成并通过验收，交付以下可复用能力：

- 系统参数管理。
- 并发安全的业务编号规则。
- Redis 认证会话、刷新轮换、令牌撤销与在线用户。
- 参数开关驱动的一次性登录验证码及响应式登录页。
- 统一数据权限、组织范围解析和用户读写约束。
- 数据变更日志、业务附件关系和 Redis 请求幂等。
- OpenAPI 统一说明、错误码文档和 MySQL 集成测试基线。
- 这些能力对应的菜单、按钮权限、前端页面、后端接口和真实数据库迁移。

M0 计划范围已全部完成；后续新增业务模块必须直接复用这些基础服务，不再自行实现平行能力。

## 2. 数据库交付

Flyway 迁移：`V2__system_parameters_and_number_rules.sql`

新增表：

- `system_parameter`：系统参数、值类型、分组、启停状态、内置标识和乐观锁版本。
- `system_number_rule`：编号前缀、日期格式、分隔符、流水位数和重置周期。
- `system_number_sequence`：按租户、规则和周期保存原子流水。

种子数据：

- `system.name`
- `system.timezone`
- `security.captcha.enabled`
- `EQUIPMENT`
- `INSPECTION_TASK`
- `MAINTENANCE_TASK`

所有表均包含租户、创建/更新人、创建/更新时间、逻辑删除和版本字段。自定义参数删除时保留逻辑删除记录，并释放原参数键供后续重新创建。

## 3. 后端交付

系统参数接口：

- `GET /api/v1/system/parameters`
- `POST /api/v1/system/parameters`
- `PUT /api/v1/system/parameters/{id}`
- `DELETE /api/v1/system/parameters/{id}`

编号规则接口：

- `GET /api/v1/system/number-rules`
- `POST /api/v1/system/number-rules`
- `PUT /api/v1/system/number-rules/{id}`
- `POST /api/v1/system/number-rules/{ruleCode}/generate`

核心约束：

- 参数键和规则编码创建后不可修改。
- 参数值按文本、布尔值、整数和小数执行后端类型校验。
- 修改操作使用版本字段实施乐观锁。
- 内置参数允许调整但不允许删除。
- 日期格式和重置周期使用白名单校验。
- 编号流水通过 MySQL 唯一索引、原子递增和事务行锁保证并发安全。
- 流水按每天、每月、每年或永不重置四种方式分段。
- 系统时区通过 `system.timezone` 参数统一读取。

## 4. 前端交付

新增页面：

- `/system/parameters`：查询、分组筛选、新增、编辑、启停和删除自定义参数。
- `/system/number-rules`：查询、新增、编辑、格式预览和生成正式编号。

页面覆盖：

- 加载状态。
- 空数据状态。
- 接口错误与重新加载。
- 按权限隐藏操作。
- 保存中防重复点击。
- 响应式表单和表格布局。
- 生成正式编号前二次确认。

新增权限：

- `system:parameter:view`
- `system:parameter:manage`
- `system:parameter:delete`
- `system:number-rule:view`
- `system:number-rule:manage`
- `system:number-rule:generate`

## 5. 验收结果

验收日期：2026-07-30

| 验收项 | 结果 |
|---|---|
| MySQL 8.0 连接 | 通过 |
| Flyway V1～V5 校验 | 通过，版本均为成功 |
| V2 种子数据 | 3 项参数、3 项编号规则、6 项菜单/按钮权限 |
| 后端启动与健康检查 | 通过，`UP` |
| 参数 API | 通过，查询、新增、修改、逻辑删除均成功 |
| 编号规则 API | 通过，查询、新增、修改和生成均成功 |
| 新规则首次编号 | 通过，首个编号为 `M0C-0001` |
| 编号并发验收 | 首次编号后再发起 100 个并发请求，得到 100 个唯一编号 |
| 数据库流水一致性 | 通过，并发段最小值 2、最大值 101、数据库当前值 101 |
| 临时验收数据清理 | 通过，临时规则和流水均已删除 |
| 后端单元测试 | 10 项通过，0 失败 |
| MySQL 集成测试 | 3 项通过，0 失败，临时数据库自动清理 |
| 前端 TypeScript 与生产构建 | 通过 |
| 前端生产依赖安全审计 | 0 个漏洞 |
| Git 空白检查 | 通过 |

编号并发验收使用独立临时规则，不占用设备、点检或维保的正式编号流水。

## 6. Redis 认证会话增量

Flyway 迁移：`V3__online_user_permissions.sql`

新增权限：

- `system:online-user:view`
- `system:online-user:kickout`

新增接口：

- `GET /api/v1/system/online-users`
- `DELETE /api/v1/system/online-users/{sessionId}`

实现约束：

- 每个访问令牌和刷新令牌均包含独立 `jti`，一对令牌共享唯一会话 `sid`。
- Redis 会话保存租户、用户、登录 IP、终端、登录时间、最近活动时间、过期时间和当前刷新令牌 ID。
- 刷新令牌通过 Lua 脚本原子校验并轮换，同一刷新令牌只允许成功使用一次。
- 检测到旧刷新令牌重用时撤销整个会话，已经轮换出的访问令牌同时失效。
- 退出登录、修改密码和管理员强制下线均写入会话黑名单，原访问令牌立即失效。
- 修改密码撤销该用户的全部旧会话，然后签发一个新会话。
- 登录失败次数使用 Redis 原子计数和 TTL，在多实例环境中保持一致。
- Redis 不可用时返回 `503 REDIS_UNAVAILABLE`，不降级为不可撤销的无状态 JWT。
- 在线用户页面展示多会话、登录 IP、终端、登录时间和最近活动时间，并禁止管理员从该页面强退自身当前会话。

真实环境验收：

| 验收项 | 结果 |
|---|---|
| Redis 版本与连接 | Redis 7.4.10，`PING` 返回 `PONG` |
| 空库迁移 | V1、V2、V3 全部成功 |
| 后端健康检查 | `UP`，包含 Redis 健康检查 |
| 首次登录与改密 | 通过，改密后原访问令牌和刷新令牌均失效 |
| 退出黑名单 | 通过，退出后原访问令牌立即返回 401 |
| 刷新令牌轮换 | 通过，新令牌可用，旧刷新令牌不可再次使用 |
| 刷新令牌重用防护 | 通过，重用后整个会话撤销 |
| 在线用户 | 两个真实会话可查询，当前会话标识唯一 |
| 强制下线 | 通过，被下线会话的访问令牌立即失效 |
| 自身强退保护 | 返回 `CANNOT_KICKOUT_SELF` |
| 分布式登录失败限制 | 连续结果为 `401,401,401,401,429,429` |
| 临时数据清理 | Redis、临时 MySQL 数据库、进程和下载文件均已清理 |

## 7. 统一数据权限增量

Flyway 迁移：`V4__data_permission_foundation.sql`

新增表：

- `organization`：企业、工厂、车间、产线和班组共用的组织树基础。
- `system_data_scope`：数据范围策略定义。
- `system_role_data_scope`：角色、范围策略和自定义组织关系。

内置范围策略：

- `ALL`：当前租户全部数据。
- `ORGANIZATION`：当前用户所属组织。
- `ORGANIZATION_AND_CHILDREN`：当前组织及全部有效下级组织。
- `SELF`：本人创建、负责或被分派的数据。
- `CUSTOM`：角色显式选择的组织集合。

新增接口：

- `GET /api/v1/system/organizations/tree`
- `GET /api/v1/system/data-scopes`
- `PUT /api/v1/system/roles/{roleId}/data-scope`

新增权限：

- `system:data-scope:view`
- `system:data-scope:manage`

实现约束：

- 用户必须绑定有效组织，用户列表返回组织名称。
- 多角色范围按并集解析；任一有效角色拥有 `ALL` 时直接使用租户全量范围。
- 角色范围每次请求从数据库解析，管理员调整后不要求目标用户重新登录。
- MyBatis 只接收结构化的当前用户 ID 和组织 ID 集合，不接收前端 SQL 或动态列名。
- 用户分页查询和总数查询使用同一数据范围片段，避免记录数泄漏。
- 用户编辑、启停和重置密码先校验目标资源，再在 `UPDATE` 语句中再次附加范围条件。
- 无任何有效范围授权时默认拒绝全部数据，不回退为租户全量。
- 自定义范围必须至少包含一个当前租户内启用的组织。
- 后续设备、点检、维保和 OEE Mapper 复用同一 `DataPermission`，按组织和负责人字段执行约束。

真实环境验收：

| 验收项 | 结果 |
|---|---|
| 独立空库迁移 | V1～V4 全部成功 |
| 正式 `leantpm` 库升级 | V4 成功，5 个组织、5 个范围策略、4 个角色范围关系 |
| 管理员组织回填 | 通过，`admin` 归属根组织 |
| 自定义组织查询 | 一线范围只返回 `scope_user`、`line_a_target` |
| 越权 ID 写操作 | 返回 `403 DATA_SCOPE_DENIED`，目标记录未修改 |
| 范围内写操作 | 通过，目标用户资料成功更新 |
| 范围实时变更 | 角色从一线改为二线后，原登录令牌下一次查询只返回 `line_b_target` |
| 前端页面 | 用户组织选择、角色范围编辑、独立数据权限页面均通过生产构建 |
| 临时数据清理 | 临时数据库、Redis、后端进程、下载文件均已清理 |

## 8. 审计、附件关系、幂等与 OpenAPI 增量

Flyway 迁移：`V5__audit_attachment_relation_permissions.sql`

新增表：

- `system_change_log`：保存资源类型、资源 ID、操作类型、变更前后 JSON、变更字段、操作人和请求 ID。
- `system_attachment_relation`：一个附件可关联多个业务记录，一个业务记录可关联多个附件。

新增接口：

- `GET /api/v1/system/change-logs`
- `POST /api/v1/system/attachments/{attachmentId}/relations`
- `DELETE /api/v1/system/attachments/relations/{relationId}`

新增权限：

- `system:change-log:view`
- `system:attachment:relation`

数据变更日志：

- 系统参数新增、修改、删除记录完整前后快照。
- 编号规则新增、修改记录完整前后快照。
- 附件绑定和解绑记录关系快照。
- `changed_fields` 由后端比较 JSON 顶层字段生成，不依赖前端声明。
- 变更日志与业务写操作处于同一事务；业务回滚时不保留虚假成功日志。
- 页面支持资源类型、关键字、日期范围、分页和双栏前后快照查看。

附件关系：

- 上传时可选自动绑定，也可在附件页面后续绑定。
- 关系类型包括 `IMAGE`、`DOCUMENT`、`MODEL` 和 `OTHER`。
- 唯一索引防止同一附件重复绑定到相同业务记录。
- 附件列表通过一次批量查询加载当前页全部关系，不产生 N+1 查询。
- 旧附件主表中的业务类型和业务 ID 已迁移到关系表，字段暂保留用于兼容。

Redis 幂等：

- 标记为幂等的写接口必须提供 `Idempotency-Key`。
- Lua 脚本原子完成首次占位，多实例同时请求只允许一个进入业务方法。
- 相同键和相同请求返回第一次的完整缓存响应，不重复写数据库。
- 相同键和不同请求返回 `409 IDEMPOTENCY_KEY_CONFLICT`。
- 执行中的重复请求返回 `409 REQUEST_IN_PROGRESS`。
- 失败请求通过比较处理令牌后释放占位，避免误删另一个实例的新状态。
- Redis 不可用时返回 `503 REDIS_UNAVAILABLE`，不绕过保护。
- 前端为写请求自动生成键，刷新令牌后的 Axios 重试保留原键。

登录验证码：

- `GET /api/v1/auth/captcha` 为公开挑战接口；参数开关关闭时仅返回 `enabled=false`。
- 开关开启后生成 4 位安全字符 SVG，答案只以 SHA-256 摘要保存到 Redis，5 分钟过期。
- 登录接口校验 `captchaId` 和 `captchaCode`；进入验证码比对后，无论正确或错误都一次性删除挑战，防止重放。
- 缺失、错误、过期或已经使用的验证码统一返回 `400 CAPTCHA_INVALID`。
- Redis 不可用时返回 `503 REDIS_UNAVAILABLE`，不会绕过验证码继续认证。
- 登录页按服务端开关动态展示验证码，失败后自动刷新挑战。
- `scripts/verify-captcha-e2e.ps1` 使用经 SHA-256 校验的临时 Redis、独立 MySQL 数据库和独立后端端口执行可复跑验收。

OpenAPI 与测试：

- Swagger UI 提供 JWT Bearer 授权、通用错误响应和幂等请求头说明。
- `docs/05-API错误码与幂等说明.md` 提供错误码、约束与调用示例。
- `scripts/run-mysql-integration.ps1` 创建唯一临时库并在 `finally` 中清理。
- 集成测试覆盖 V1～V5 空库迁移、递归组织范围和 MySQL JSON 变更快照。

真实环境验收：

| 验收项 | 结果 |
|---|---|
| 独立空库迁移 | V1～V5 全部成功 |
| 正式 `leantpm` 库升级 | V5 成功，两项新权限完成授权 |
| 缺少幂等键 | 返回 `400 IDEMPOTENCY_KEY_REQUIRED`，未产生业务记录 |
| 相同键相同参数 | 两次响应业务 ID 相同，数据库仅 1 条参数记录 |
| 相同键不同参数 | 返回 `409 IDEMPOTENCY_KEY_CONFLICT` |
| 参数审计 | 新增和修改产生 2 条日志，修改字段包含 `parameterValue` |
| 附件上传幂等 | 两次上传响应附件 ID 相同，数据库仅 1 个附件 |
| 附件绑定/解绑 | 关系正确创建和删除，并产生 `BIND`、`UNBIND` 两条日志 |
| OpenAPI | 公开验证码不要求令牌；用户资料要求 Bearer；幂等写接口包含必填 `Idempotency-Key` |
| MySQL 集成测试 | 3 项通过，0 失败 |
| 验证码默认状态 | 参数默认关闭，公开挑战接口返回 `enabled=false` |
| 验证码开关 | 参数开启后，缺少验证码返回 `CAPTCHA_INVALID` |
| 验证码正确登录 | 自动解析真实 SVG 验证码后登录成功 |
| 验证码防重放 | 同一挑战第二次使用返回 `CAPTCHA_INVALID` |
| 后端单元测试 | 12 项通过，0 失败；另有 3 项按环境开关跳过 |
| 前端生产构建 | Vue 类型检查与 Vite 构建通过 |
| 临时资源清理 | 临时数据库、Redis、后端、下载包和上传文件均已清理 |

## 9. 已知事项与下一阶段

- 前端主包当前约 1.2 MB，Vite 给出大包提醒；将在可视化模块进入前实施依赖分包。
- Mockito 在 Java 21 下提示未来版本将限制动态代理加载，当前不影响测试结果；后续将配置显式测试代理。
- 本机未常驻安装 Redis；开发或运行系统前必须启动 Redis 7.x。
- 下一阶段进入 M1.1「组织与位置」，在当前只读组织树基础上补齐完整 CRUD、引用校验和物理位置树。
