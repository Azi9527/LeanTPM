# 清理门禁报告

执行时间：2026-08-08T16:47:35+08:00

本报告仅覆盖本任务能够证明归属的生成物。没有执行 `git clean`、通用递归清理、stash、服务/数据库清理或用户文件删除。

| 目标 | 所有者/归属证据 | 清理方式 | 结果 |
|---|---|---|---|
| 工作树 `.tmp/npm-cache` | 本任务聚合验证创建；位于新工作树且创建时间为 2026-08-08 | 核验绝对路径位于工作树后按精确路径删除 | REMOVED；空 `.tmp` 同步删除 |
| `frontend/node_modules` | 本任务 `npm ci` 创建；新工作树内创建时间为 2026-08-08 | 核验绝对路径后按精确路径删除 | REMOVED |
| `frontend/dist` | 本任务 Vite 构建创建；新工作树内创建时间为 2026-08-08 | 核验绝对路径后按精确路径删除 | REMOVED |
| `backend/target` | 本任务 Maven 回归创建；新工作树内创建时间为 2026-08-08 | 核验绝对路径后按精确路径删除 | REMOVED |
| `LeanTPM-APP/unpackage` | 正式 HBuilder 构建未执行 | 只读检查 | ABSENT |
| `leantpm-package-*` OS 临时目录 | 发布包测试的 `finally` 清理合同 | 只读残留检查 | 0 个残留 |
| 报告秘密特征 | 本任务证据目录 | 只读扫描私钥头、Bearer、常见云/GitHub Token 与 Cookie 特征 | 0 个匹配文件 |

## 明确保留的资源

- OS 临时目录中的 `leantpm-npm-cache`：虽然名称与本轮聚合命令一致，但其位于共享用户临时目录，无法仅凭名称证明不存在其他所有者，故未删除。
- 2026-08-03 至 2026-08-05 创建的其他 `leantpm-*` 临时目录：早于本工作树任务，归属不明，未删除。
- 本任务未启动数据库、Windows Service、Caddy、后端进程或监听端口，因此无相关清理动作。

清理后复核：上述工作树生成目录全部不存在；发布包临时目录为 0；后续关键发布合同与 PowerShell 解析门禁再次执行。

## 19:22 无 Redis 候选复核后的增量清理

- 本轮 Maven 使用专属 `backend/target-codex-final`，确认绝对路径位于当前工作树后已精确删除；
- 本轮 Vite 重新生成 `frontend/dist`，确认绝对路径位于当前工作树后已精确删除；
- `frontend/node_modules` 在本轮开始前已经存在，当前轮未执行 `npm ci`，无法重新证明独占归属，因此保留；
- 未执行 `git clean`、通用递归清理、数据库/服务/网络清理或用户文件处理。
